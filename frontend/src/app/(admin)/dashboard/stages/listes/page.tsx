import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Liste des Stages",
  description: "Consulter la liste des offres de stage",
};

export default function StagesListPage() {
  return (
    <div className="container mx-auto py-6">
      <h1 className="text-2xl font-bold mb-6">Liste des offres de stage</h1>
      {/* Tableau des stages à implémenter */}
    </div>
  );
} 